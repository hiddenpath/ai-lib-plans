# Daily Standup - 2026-03-10

## 今日完成

1. `GO-002` 四项任务推进（第二轮）：
   - 错误分类从仅状态码升级为 `provider code/type + status` 双通道
   - 补齐 retry/fallback 判定矩阵
   - 增加 transport preflight 校验（path/method/id/messages）
   - 增补高级能力契约测试（mcp/computer_use/reasoning/video）

2. `ai-lib-go` 代码更新：
   - `pkg/ailib/client.go`：新增 preflight、provider error 分类、fallback 判定辅助函数
   - `pkg/ailib/errors.go`：扩展 `E2002/E4001/E4002` 常量与状态映射
   - `pkg/ailib/client_test.go`：新增高级能力路由/错误分类/preflight 测试
   - `tests/compliance/advanced_capabilities_test.go`：新增高级能力契约测试

3. 差异报告：
   - 新增 `active/projects/ai-lib-go/GO-002-cross-runtime-gap-report-2026-03-10.md`
   - 记录已对齐项与剩余差距，作为 GO-002 下阶段输入

4. `GO-002` 追加推进（第三轮）：
   - 完成 manifest 驱动错误分类读取（`error_classification`）
   - 新增最小可用 fallback 执行器：`FallbackClient.Chat`
   - 增补对应单测：manifest 分类覆盖 + fallback 成功转移路径
   - 回归通过：`go test ./...`

5. `GO-002` 追加推进（第四轮）：
   - 将 fallback 执行器扩展到完整 `Client` 能力面（含 stream 建连失败转移）
   - 增补 `ChatStream` failover 回归测试
   - 回归通过：`go test ./...`

6. `GO-002` 追加推进（第五轮）：
   - 在 `ai-protocol` 新增共享夹具分类：`07-advanced-capabilities`
   - 在 `ai-lib-go` compliance runner 接入该分类执行
   - 回归通过：`go test ./...`

7. `GO-002` 追加推进（第六轮）：
   - 将 `07-advanced-capabilities` 扩展为 12 条完整矩阵（guard/endpoint/fallback）
   - 同步 Rust/Python/TS compliance runner 可消费新 `input.type`
   - 验证：
     - Rust: `cargo test --test compliance` 通过
     - Python: `pytest tests/compliance/test_compliance.py` 通过（含 `adv-012` 定点）
     - Go: `go test ./tests/compliance -run TestAdvancedCapabilitiesCompliance -v` 通过
     - TS: `vitest` 通过（修复 JSON BOM 导致的配置解析阻塞后）

8. 收尾目标补充：
   - 新增 `GO-003`（矩阵仓库 commit/push + 运行时包发布 + `ailib.info` 对齐）
   - 明确 Go 包发布为最终验收项之一（模块代理可安装）

9. `GO-002` 追加推进（第七轮）：
   - `07-advanced-capabilities` 扩展到 provider-mock 行为断言（请求/响应体）
   - Go fallback 升级为健康度 + 熔断策略（threshold/open-window/snapshot）
   - Rust/Python/TS/Go 四端 runner 均已验证可消费新增行为类型

## 明日计划

1. 在 fallback 执行器中引入 provider ranking/health/circuit-breaker 策略。
2. 兼容更多历史 manifest 形态下的错误分类字段读取。
3. 完成 `GO-003` 依赖条件后执行跨仓库提交推送与多语言包发布。

## 风险与阻塞

- 风险：高级能力在 ai-protocol 侧尚无统一 fixture taxonomy，可能造成跨运行时验证口径漂移。
- 缓解：先保持 Go 侧契约测试可执行，再提交统一夹具提案并同步多运行时消费。

## 发布执行补充（夜间收尾）

1. 矩阵仓库已完成串行 commit/push + tag：
   - ai-protocol `v0.8.3`
   - ai-lib-go `v0.0.1`
   - ai-lib-rust `v0.9.3`
   - ai-lib-python `v0.8.3`
   - ai-lib-ts `v0.5.3`
   - ai-protocol-mock `v0.1.11`
   - spiderswitch `v0.4.2`
   - ailib.info `v0.7.1`

2. 运行时包发布校验：
   - npm: `@hiddenpath/ai-lib-ts@0.5.3` 可见
   - crates.io: `ai-lib-rust@0.9.3` 可见
   - PyPI: `ai-lib-python@0.8.3` / `ai-protocol-mock@0.1.11` / `spiderswitch@0.4.2` 可见
   - Go: `github.com/hiddenpath/ai-lib-go@v0.0.1` 模块代理可解析

3. 站点对齐：
   - `ailib.info` 已完成 EN/ZH/JA/ES 的 intro+ecosystem 页面 Go runtime 与版本矩阵同步。

