id: PR-P1-006
title: "Docker deployment + Caddy TLS + api.prism.ailib.info"
status: completed ✅
priority: P0
project: prism
phase: 1
scope: product
repo: "hiddenpath/ai-lib-gateway"
depends_on: ["PR-P1-002"]
assignee: "alex"
executor_name: "alex"
executor_terminal: "cursor-agent"
created: "2026-04-25"
updated: "2026-06-21"

description: |
  - Reuse/adapt eos deploy template (`ai-lib-plans/tools/deploy_eos.sh`, Dockerfile, Caddy)
  - Multi-stage Dockerfile, docker-compose (gateway + caddy)
  - Caddyfile for api.prism.ailib.info
  - SQLite volume persistence
  - DNS: PR-P1-013
  - Does **not** require fallback routing (PR-P1-005) to be product-complete

acceptance_criteria:
  - `docker compose up` starts gateway + caddy
  - `https://api.prism.ailib.info/health` returns 200 (after PR-P1-013 DNS + TLS)
  - Auto-restart on failure; SQLite data persists across restarts
  - TLS auto-provisioned by Caddy

testing:
  strategy: "Manual: deploy to VPS, curl external endpoint (may share 43.159.226.236 with Eos or separate host — document in evidence)"
  evidence: "PR #5 85b7c42 local stack; PR #7 b1b0b69 production overlay + DEPLOY.md; HTTPS acceptance after PR-P1-013 DNS"

pr:
  url: "https://github.com/hiddenpath/ai-lib-gateway/pull/7"
  merge_commit: "b1b0b69"

completion_notes: |
  PR #5 (85b7c42): local/staging compose stack + Caddy local profile + .netrc build secret.
  PR #7 (b1b0b69): production TLS stack — docker-compose.production.yml overlay, deploy/Caddyfile for api.prism.ailib.info, config/production.example.toml (5 providers), verify-production.sh, DEPLOY.md.
  PR #8 (50ee5c0): corrected .env naming (Docker Compose auto-loads .env, not .env.production), added Path B1 (Eos Caddy fronting Prism on loopback), comprehensive troubleshooting table.
  Production acceptance (HTTPS) still blocked on PR-P1-013 (DNS).
