id: PP-001
title: "Pack contract draft: JSON Schema extension + 1 example"
status: in_progress
priority: P2
project: prism
phase: wave2
depends_on: ["PR-P1-008"]
assignee: "cursor"
executor_name: "cursor"
executor_terminal: "agent-session"
created: "2026-04-26"
updated: "2026-06-24"
description: |
  ⚠️ Repo: `hiddenpath/gateway` — C-band closed-source; do NOT push to `ailib-official` public org.
  Draft the Prism Pack capability contract:
  - Define JSON Schema for Pack manifest (extends ai-protocol v2 manifest)
  - Pack fields: id, name, version, provider_routes, signing_info, metadata
  - provider_routes: array of {provider, model, priority, cost_weight, capability_tags}
  - Create 1 concrete example: "deepseek-economy-pack" (routes to DeepSeek models with cost optimization)
  - Document in docs/ or ai-protocol repo under "Pack Specification" section
  - NOT a marketplace implementation — just the contract/schema
acceptance_criteria:
  - JSON Schema validates against example pack manifest
  - Example pack is self-documenting (comments or README)
  - Schema references ai-protocol v2 manifest where applicable
  - No marketplace or registry implementation (contract only)
testing:
  strategy: "Validate: example JSON against schema; review schema completeness against report 5 §6 Pack description"
  evidence: "npm run validate:packs passes locally (1 example); full validate 108 passed"
pr:
  url: "https://github.com/ailib-official/ai-protocol/pull/10"
  merge_commit: ""
completion_notes: ""
