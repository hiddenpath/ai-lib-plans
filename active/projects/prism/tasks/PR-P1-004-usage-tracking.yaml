id: PR-P1-004
title: "Usage tracking (SQLite + Pricer)"
status: completed
priority: P0
project: prism
phase: 1
scope: library
repo: "hiddenpath/eos — crates/prism-core"
depends_on: ["PR-P1-002-LIB"]
assignee: ""
executor_name: "cursor-agent"
executor_terminal: "cursor-prism-review"
created: "2026-04-25"
updated: "2026-06-04"

description: |
  Library-layer usage in prism-core (`usage.rs`):
  - UsageRecord, SqliteUsageStore, Pricer (static pricing table)
  - Metadata only — no conversation content
  - `GET /admin/usage` HTTP: PR-P1-014
  - Tech debt: `cost_usd: f64` → migrate to µUSD before Phase 2 billing

acceptance_criteria:
  - Pricer calculates cost from pricing table + token counts
  - SQLite database auto-created on first run (library tests)
  - Query/filter API available via AdminService / store traits
  - No conversation content stored
  - `cargo test -p prism-core --features full` passes usage tests

testing:
  strategy: "Unit tests for Pricer + SqliteUsageStore"
  evidence: "2026-06-04: cargo test --features full → 45 passed (usage.rs tests)"

pr:
  url: ""
  merge_commit: ""

completion_notes: |
  Library complete. HTTP query endpoint and per-request auto-record on live traffic = PR-P1-002 + PR-P1-014.
