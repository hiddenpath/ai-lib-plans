id: PR-P2-001
title: "Pricing precision — migrate UsageRecord to µUSD (i64)"
status: in_progress
priority: P1
project: prism
phase: 2
scope: library
repo: "hiddenpath/eos — crates/prism-core"
depends_on: ["PR-P1-004"]
assignee: "cursor"
executor_name: "cursor"
executor_terminal: "agent-session"
created: "2026-06-22"
updated: "2026-06-23"
description: |
  Phase 2 billing 先决：将 `UsageRecord.cost_usd: f64` 迁为 `cost_micro_usd: i64`。
  Pricer 内部整数运算；API 边界可选只读 `cost_usd` 换算字段。
  见 prism-core `usage.rs` 模块级 µUSD 注释。
acceptance_criteria:
  - UsageRecord 持久化使用 i64 microdollars
  - Pricer::calculate_cost 返回 microdollars 或新类型
  - 现有 usage 单测更新；累加无浮点漂移回归测试
  - Admin usage JSON 文档说明字段变更
testing:
  strategy: "cargo test -p prism-core --features full usage"
  evidence: "6 usage/admin tests pass locally; clippy clean"
pr:
  url: ""
  merge_commit: ""
completion_notes: ""
