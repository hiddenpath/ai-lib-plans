id: PR-P1-005
title: "Fallback routing (primary provider fail → backup switch)"
status: completed
priority: P0
project: prism
phase: 1
scope: library
repo: "hiddenpath/eos — crates/prism-core"
depends_on: ["PR-P1-002-LIB", "PR-P1-003"]
assignee: ""
executor_name: "cursor-agent"
executor_terminal: "cursor-prism-review"
created: "2026-04-25"
updated: "2026-06-04"

description: |
  Library-layer router in prism-core (`router.rs`):
  - Fallback order, retryable vs non-retryable errors, provider health
  - Optional region filter (`route_with_region`) — see EOS-ARCH-001

acceptance_criteria:
  - Primary 5xx → retry on backup provider (library tests)
  - Auth 401/403 does NOT trigger fallback
  - Health tracker updates on each result
  - `cargo test -p prism-core --features full` passes router tests

testing:
  strategy: "Integration test: mock primary failure, verify fallback to secondary"
  evidence: "2026-06-04: cargo test --features full → 45 passed (router.rs tests)"

pr:
  url: ""
  merge_commit: ""

completion_notes: |
  Library complete. E2E via Prism HTTP API validated under PR-P1-008.
