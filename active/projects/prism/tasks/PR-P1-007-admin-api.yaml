id: PR-P1-007
title: "Admin service (keys/users/usage CRUD logic)"
status: completed
priority: P1
project: prism
phase: 1
scope: library
repo: "hiddenpath/eos — crates/prism-core"
depends_on: ["PR-P1-003", "PR-P1-004"]
assignee: ""
executor_name: "cursor-agent"
executor_terminal: "cursor-prism-review"
created: "2026-04-25"
updated: "2026-06-04"

description: |
  Library-layer AdminService in prism-core (`admin.rs`):
  - CRUD for keys, users, usage queries, health summaries
  - Axum route binding: **PR-P1-014** (product)

acceptance_criteria:
  - AdminService unit tests pass under `cargo test -p prism-core --features full`
  - CRUD operations on keys and users work at service layer
  - Usage query supports date range + user_id + provider_id filters (library API)
  - Health summary available for per-provider stats

testing:
  strategy: "Unit tests for AdminService"
  evidence: "2026-06-04: cargo test --features full → 45 passed (admin.rs tests)"

pr:
  url: ""
  merge_commit: ""

completion_notes: |
  Library complete. HTTP `/admin/*` + admin token middleware = PR-P1-014.
