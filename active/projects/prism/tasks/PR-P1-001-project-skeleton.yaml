id: PR-P1-001
title: "Gateway shell — Axum + TOML config + /health"
status: completed
priority: P0
project: prism
phase: 1
scope: product
repo: "hiddenpath/ai-lib-gateway"
depends_on: []
assignee: "alex"
executor_name: "alex"
executor_terminal: "cursor-agent"
created: "2026-04-25"
updated: "2026-06-07"

description: |
  Product-layer gateway shell (not prism-core library internals):
  - Axum server with `GET /health` (version + readiness)
  - TOML config loader at startup (provider list, keys, ports) — see PR-P1-009 for library loader
  - Depends on prism-core crate (path or published), **not** ai-lib-core as Phase 1 requirement
  - Eos today: `/health` exists; still missing unified `config.toml` + Prism-oriented layout

acceptance_criteria:
  - `cargo build` succeeds with prism-core dependency
  - `GET /health` returns 200 with version info
  - Config loads from `config.toml` at startup (or documents EnvConfig fallback)
  - Docker skeleton present or documented reuse of eos deploy template

testing:
  strategy: "cargo test + manual curl /health"
  evidence: "ai-lib-gateway CI green; GET /health unit test; merge e193933 chain includes P1-001 f80decb"

pr:
  url: "https://github.com/hiddenpath/ai-lib-gateway/pull/1"
  merge_commit: "f80decb7807e7f875929c38ddaeef38ac988c0fb"

completion_notes: |
  Axum shell, TOML/EnvConfig loader, GET /health, Dockerfile skeleton.
  OpenCode review: BIZ-002 UNLICENSED, prism-core git pin, CI eos checkout+patch.
