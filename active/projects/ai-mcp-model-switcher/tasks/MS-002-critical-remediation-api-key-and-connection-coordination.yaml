---
id: "MS-002"
title: "严格修复：API Key 安全、连接协调与稳定性治理"
status: "completed"
priority: "critical"
assignee: "@hiddenpath"
project: "ai-mcp-model-switcher"
milestone: "v0.1.1"
labels:
  - "security"
  - "reliability"
  - "mcp"
  - "governance"
created: "2026-03-02"
updated: "2026-03-02"

description: |
  基于严格代码审查结论，执行一次结构化修复专项，目标是把项目从演示级
  提升到可发布的最小安全与稳定基线。范围覆盖：
  1) 用户 API key 的设置、校验、错误提示与文档引导
  2) MCP 模型切换连接与 agent 侧连接管理的协调机制
  3) P0/P1 缺陷修复与测试补强

acceptance_criteria:
  - "移除/限制明文 api_key 入参造成的泄露风险，并增加日志脱敏"
  - "修复 server/state/runtime/base/__init__ 中的重复定义与错误路径"
  - "修复可执行入口，确保 console script 可正常启动"
  - "新增连接协调元数据（connection_epoch / last_switched_at）并对外可见"
  - "README 与 README_CN 提供完整 API key 配置指南与排障流程"
  - "完成 ruff、mypy、pytest，并在 testing 字段记录结果"

dependencies:
  - "MS-001"
related_issues: []
references: []
estimated_hours: 8
actual_hours: 6
blocks: []
blocked_by: []

files_changed:
  - "src/ai_mcp_model_switcher/server.py"
  - "src/ai_mcp_model_switcher/state.py"
  - "src/ai_mcp_model_switcher/runtime/base.py"
  - "src/ai_mcp_model_switcher/runtime/python_runtime.py"
  - "src/ai_mcp_model_switcher/validation.py"
  - "src/ai_mcp_model_switcher/response.py"
  - "src/ai_mcp_model_switcher/__init__.py"
  - "src/ai_mcp_model_switcher/tools/switch.py"
  - "src/ai_mcp_model_switcher/tools/list.py"
  - "src/ai_mcp_model_switcher/tools/status.py"
  - "pyproject.toml"
  - "README.md"
  - "README_CN.md"
  - "tests/test_validation.py"
  - "tests/test_state.py"

testing:
  unit_tests:
    - "python -m pytest -q -> 16 passed"
  integration_tests:
    - "python -m ruff check src tests -> All checks passed"
    - "python -m mypy src -> Success: no issues found in 13 source files"
  compliance_tests: []

completion_notes: |
  已完成 P0/P1 严格修复并通过本地验证：
  1) 安全：日志参数脱敏；API key 缺失时返回可操作提示（provider + expected_env_vars）
  2) 稳定：修复重复定义、未知工具错误路径、CLI entrypoint 指向错误
  3) 协调：状态中新增 connection_epoch / last_switched_at，支持 agent 侧同步
  4) 架构：移除 provider 白名单硬编码，模型清单改为从 ai-protocol v1/models 动态装载（[ARCH-001]）
  5) 质量：ruff + mypy + pytest 全通过

tech_considerations:
  architecture: |
    遵循 ARCH-001，避免硬编码 provider 逻辑；提供连接协调元数据而非耦合 agent 内部实现。

  performance: |
    切换路径增加轻量锁与状态版本号，不引入重依赖与跨进程同步开销。

  security: |
    禁止敏感参数进入日志；默认引导环境变量密钥配置；保留最小必要错误信息。

  compatibility: |
    变更优先兼容现有工具调用；对潜在破坏性行为使用清晰错误提示与迁移文档。

constitution_rules:
  - "ARCH-001: Protocol-driven design"
  - "ARCH-003: Cross-runtime consistency"
  - "DOC-001: Documentation language requirements"
  - "TEST-001: Compliance tests"
---
