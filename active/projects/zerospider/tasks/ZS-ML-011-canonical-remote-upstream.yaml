---
id: "ZS-ML-011"
title: "Canonical Git remote: ailib-official as default + main tracks official main"
status: "pending"
priority: "high"
assignee: "@maintainer"
project: "zerospider"
milestone: "ai-lib-migration-phase7"
labels:
  - "zerospider"
  - "governance"
  - "git-hygiene"
created: "2026-05-08"
updated: "2026-05-08"
description: |
  Local and team clones must treat `ailib-official/zerospider` as the **default** push/pull
  remote. Today many clones still use `origin -> hiddenpath` while `main` tracks that
  upstream, causing false ahead/behind and accidental pulls from archived remotes.

  Deliver either:
  - (A) rename remotes so `origin` is ailib-official; or
  - (B) keep two remotes but set `main` upstream to `ailib-official/main` only.

  Optional small PR: CONTRIBUTING / ai-lib-migration doc one-liner on canonical clone URL.

acceptance_criteria:
  - "`git branch -vv` shows main tracking ailib-official/main (or origin after rename)"
  - "`git status -sb` on clean main matches official (no spurious ahead 29 vs hiddenpath)"
  - "Documented canonical URL in-repo if CONTRIBUTING exists"

pr:
  repo: "ailib-official/zerospider"
  merge_target: "main"
  branch_pattern: "chore/zs-ml-011-canonical-remote-docs"

testing:
  evidence_on_complete:
    - "Paste output of: git remote -v; git branch -vv; git rev-parse main ailib-official/main"
  commands_must_pass:
    - "git rev-parse main"
    - "git rev-parse ailib-official/main"
    - "# expect: equal SHAs when synced"

related_plan: "active/projects/zerospider/ZEROSPIDER_PHASE7_LEGACY_ELIMINATION_PLAN_2026-05-08.md#zs-ml-011--远端与跟踪分支"
