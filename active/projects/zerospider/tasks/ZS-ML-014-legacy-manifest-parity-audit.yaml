---
id: "ZS-ML-014"
title: "Audit: every legacy factory arm has ai-protocol manifest parity (or explicit unsupported)"
status: "in_progress"
priority: "critical"
executor_name: "composer-agent"
executor_terminal: "cursor-agent-p7-zsml"
assignee: "@ai"
project: "zerospider"
milestone: "ai-lib-migration-phase7"
labels:
  - "zerospider"
  - "ai-protocol"
  - "audit"
created: "2026-05-08"
updated: "2026-05-08"
description: |
  Before deleting `legacy-providers`, enumerate each legacy resolution path (string keys,
  `custom:`, etc.) and ensure **equivalent** provider definitions exist in ai-protocol
  (PR to ai-protocol) OR mark **unsupported** with user-facing error + issue link.

  Deliverable: `docs/internal/legacy-manifest-parity.md` in zerospider OR plans artifact
  under ai-lib-plans with table + links.

  Add ≥1 ZeroSpider integration test: protocol-only (`ai-protocol` without
  `legacy-providers`) resolves a provider that previously needed legacy **using**
  checked-in or generated minimal fixture under `AI_PROTOCOL_DIR`.

acceptance_criteria:
  - "Coverage table: no row remains 「only works with legacy」 unless explicitly unsupported"
  - "ai-protocol PR links for added manifests (if any) merged or open with owner ack"
  - "New test passes with `--features ai-protocol` only"

pr:
  repo: "ailib-official/zerospider"
  url: "https://github.com/ailib-official/zerospider/pull/31"
  merge_target: "main"
  branch_pattern: "feat/zs-ml-phase7-011-014-protocol-ci"
  merge_commit: ""
  related_repos:
    - "ailib-official/ai-protocol"

completion_notes: |
  Parity ledger: `docs/internal/legacy-manifest-parity.md`; integration test `tests/protocol_manifest_parity.rs`; fixture synced from canonical `openai.yaml` upstream.

testing:
  commands_must_pass:
    - "cargo test -p zerospider --features ai-protocol --locked --test protocol_manifest_parity"
  evidence_local_2026_05_08:
    - "`protocol_fixture_resolves_openai_without_legacy` ok (fixture under tests/fixtures/ai-protocol-min)."

blocks:
  - "ZS-ML-015"
blocked_by: []

related_plan: "active/projects/zerospider/ZEROSPIDER_PHASE7_LEGACY_ELIMINATION_PLAN_2026-05-08.md#zs-ml-014--legacy-覆盖审计manifest-真源"
