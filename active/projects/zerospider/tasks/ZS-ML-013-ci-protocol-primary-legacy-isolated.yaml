---
id: "ZS-ML-013"
title: "CI: required jobs protocol-only; legacy-providers in isolated regression job(s)"
status: "in_progress"
priority: "high"
executor_name: "composer-agent"
executor_terminal: "cursor-agent-p7-zsml"
assignee: "@ai"
project: "zerospider"
milestone: "ai-lib-migration-phase7"
labels:
  - "zerospider"
  - "ci"
  - "ai-protocol"
created: "2026-05-08"
updated: "2026-05-08"
description: |
  Merge gate must not implicitly require `legacy-providers` for default correctness.

  Refactor `.github/workflows/*.yml` so **required** matrix uses `--features ai-protocol`
  (matching `default`). Add explicit **legacy-regression** job that runs
  `cargo test … --features \"ai-protocol legacy-providers\"` until ZS-ML-015 removes legacy.

  Document local commands for contributors touching `providers/mod.rs` legacy arms.

acceptance_criteria:
  - "Branch protection / merge requirements attach only to protocol-primary jobs"
  - "Legacy job still exists and passes on main until ZS-ML-015"
  - "CONTRIBUTING or docs mention when to run legacy command locally"

pr:
  repo: "ailib-official/zerospider"
  url: "https://github.com/ailib-official/zerospider/pull/31"
  merge_target: "main"
  branch_pattern: "feat/zs-ml-phase7-011-014-protocol-ci"
  merge_commit: ""

completion_notes: |
  CI jobs renamed to `rust-protocol-required` vs `rust-legacy-regression` in umbrella PR #31.
  Maintainer: align branch-protection required checks to the new protocol job names.

testing:
  evidence_on_complete:
    - "Awaiting Actions URL on merged main (two jobs: rust-protocol-required + rust-legacy-regression)."
  evidence_local_2026_05_08:
    - "Contributing documents `cargo test -p zerospider --features \"ai-protocol legacy-providers\" --locked` for legacy touchers."
    - "Local equivalent of protocol job exercised via clippy/tests with `--features ai-protocol --locked`."

related_plan: "active/projects/zerospider/ZEROSPIDER_PHASE7_LEGACY_ELIMINATION_PLAN_2026-05-08.md#zs-ml-013--ciail-protocol-为主legacy-providers-为隔离回归"
