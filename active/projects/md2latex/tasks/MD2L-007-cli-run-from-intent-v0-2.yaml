---
id: "MD2L-007"
title: "CLI: single-shot run from intent (plan + run)"
status: "completed"
priority: "medium"
assignee: "@ai"
project: "md2latex"
milestone: "v0.2.x"
labels:
  - "cli"
  - "planner"
created: "2026-03-26"
updated: "2026-03-26"
executor_name: "cursor-agent"
executor_terminal: "cursor-shell"

description: |
  子命令或 flag：在同一进程内先 plan_app_file_v0_1_from_intent，再对输入 markdown 执行 run，
  可选将中间 YAML 写到临时或指定路径。

acceptance_criteria:
  - "一条命令完成 intent -> graph -> run（网络与 policy 与现有一致）"
  - "可选保留生成的 app YAML 供审阅"

dependencies:
  - "MD2L-004"

estimated_hours: 4
actual_hours: 1
blocks: []
blocked_by: []

files_changed:
  - "d:/rustapp/md2latex/src/cli.rs"
testing:
  cli_checks:
    - "cargo run -- run-intent --help"
  full_validation:
    - "cargo clippy --all-targets -- -D warnings"
    - "cargo test"
completion_notes: |
  Added `run-intent` command: intent -> planner -> run in one invocation.
  Supports `--save-planned-app` and reuses existing run options (`--trace`, `--step-stdin`, `--gate-patch-file`, vars dump flags).
---
