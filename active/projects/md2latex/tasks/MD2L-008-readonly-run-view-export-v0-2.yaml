---
id: "MD2L-008"
title: "Readonly run view export (graph + node statuses)"
status: "completed"
priority: "medium"
assignee: "@ai"
project: "md2latex"
milestone: "v0.2.x"
labels:
  - "observability"
  - "human-in-the-loop"
  - "cli"
created: "2026-03-26"
updated: "2026-03-26"
executor_name: "cursor-agent"
executor_terminal: "cursor-shell"

description: |
  输出“只读运行视图”JSON：包含 graph 节点清单与每节点运行状态（started/completed/ok/error），
  供外部 UI / Execution Panel 消费。通过 CLI `--export-view` 暴露。

acceptance_criteria:
  - "存在 view 构建函数：app schema + trace events -> readonly JSON model"
  - "run / run-intent 均支持 --export-view PATH"
  - "测试覆盖 view 构建逻辑；CLI help 可见新参数"

dependencies:
  - "MD2L-007"

references:
  - "active/projects/md2latex/MD2L_APP_PROTOCOL_MIN_CONTRACT_2026-03-25.md"

estimated_hours: 4
actual_hours: 2
blocks: []
blocked_by: []

files_changed:
  - "d:/rustapp/md2latex/src/view.rs"
  - "d:/rustapp/md2latex/src/trace.rs"
  - "d:/rustapp/md2latex/src/cli.rs"
  - "d:/rustapp/md2latex/src/lib.rs"
testing:
  unit_tests:
    - "view::tests::build_view_marks_node_status"
  cli_checks:
    - "cargo run -- run --help"
    - "cargo run -- run-intent --help"
  full_validation:
    - "cargo clippy --all-targets -- -D warnings"
    - "cargo test"
completion_notes: |
  Added readonly view model + builder (`src/view.rs`), event collector + tee observer (`src/trace.rs`),
  and CLI `--export-view` for both run paths.
---
