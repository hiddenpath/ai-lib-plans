---
id: "PT-075"
title: "V2-alpha 模型上下文容量元数据与 V1 对等（context_window / max_output）"
status: "in_progress"
priority: "high"
executor_name: "alex"
executor_terminal: "cursor-agent"
assignee: "@ai"
project: "ai-protocol"
milestone: "v2-alpha-capacity"
labels:
  - "ai-protocol"
  - "v2-alpha"
  - "manifest"
  - "compliance"
created: "2026-05-22"
updated: "2026-06-02"
description: |
  为策略层与运行时容量感知路由提供**可机器消费**的真源：在 V2-alpha provider / model 定义中恢复并规范
  `context_window` 与 `max_output`（或与 v1 `pricing`/`models` 对齐的 max output tokens 字段名，须与 schema 一致），
  与 `dist/v1/models/**` 对齐；增加合规测试防止 v1 有而 v2 丢的回归。

  **显式不做**：在协议中固化压缩、shaping、routing 策略或 ContextProfile（见 `ai-lib-plans/MEMORY.md` 2026-05-22）。

  **下游依赖**：`ALR-P2-001`（共享组装）、`EOS-P2-001`（浏览器宿主）在完成本任务定义的 schema + 数据迁徙后再实现消费侧。

blocks:
  - id: "PT-075-R1"
    title: "schemas/v2/provider.json：模型级整数 capacity 字段"
    status: "completed"
    description: |
      ai-protocol PR #4 (0f1cfb2) 已合并：`metadata-model-entry.json` + provider.json metadata.models ref；load-017/018。
      为每个 model 条目增加 `context_window`、`max_output_tokens`；0 = unknown。
    depends_on: []
  - id: "PT-075-R2"
    title: "dist/v2-alpha/providers/*.json：从 v1 迁移数值"
    status: "completed"
    description: "ai-protocol PR #5 (8740d21) 已合并：自 dist/v1/models/** 映射 context_window、max_output；load-019–021。"
    depends_on: ["PT-075-R1"]
  - id: "PT-075-R3"
    title: "dist/v2-alpha/spec.json 文档化 capacity 语义"
    status: "in_progress"
    description: |
      ai-protocol PR #6：`model_capacity_metadata` section in spec.json。
      standard_capabilities 仍为能力名；context window 为 model 级 metadata.models 整数。
    depends_on: ["PT-075-R1"]
  - id: "PT-075-R4"
    title: "合规测试：v1↔v2 模型容量对等"
    status: "in_progress"
    description: "ai-protocol PR #6：`scripts/verify-v1-v2-capacity-parity.js` + npm run verify:v1-v2-capacity (12 models)。"
    depends_on: ["PT-075-R2"]
testing:
  - "JSON Schema 校验全量 v2-alpha providers"
  - "v1↔v2 容量字段矩阵测试"
blocks_summary:
  total: 4
completion_notes: |
  2026-05-31: R1 merged (ai-protocol PR #4, 0f1cfb2).
  2026-06-01: R2 merged (ai-protocol PR #5, 8740d21). ai-lib-rust compliance runner path fix (PR #5, 8dfa9eb) unblocked load-017–021 on main.
