---
id: "PT-008"
title: "Release ai-protocol-mock v0.1.8 and run runtime smoke validation"
status: "completed"
priority: "high"
assignee: "@hiddenpath"
project: "ai-protocol"
milestone: "v0.8.0"
labels:
  - "release"
  - "mock"
  - "runtime-validation"
created: "2026-02-28"
updated: "2026-02-28"

description: |
  Execute two follow-up tasks after protocol/mocks alignment:
  1) publish ai-protocol-mock release v0.1.8
  2) run minimal integration checks in ai-lib-rust, ai-lib-python, ai-lib-ts
  against mock + v2 provider-related path to confirm no regressions.

acceptance_criteria:
  - "ai-protocol-mock version bumped to 0.1.8 and pushed"
  - "GitHub release v0.1.8 created with concise notes"
  - "Minimal runtime integration tests executed for rust/python/ts"
  - "Results and any follow-up risks documented"

dependencies:
  - "PT-006"
  - "PT-007"

related_issues: []

references:
  - "https://github.com/hiddenpath/ai-protocol-mock/releases/tag/v0.1.8"

estimated_hours: 2
actual_hours: 2

blocks: []
blocked_by: []

files_changed:
  - "ai-protocol-mock/pyproject.toml"
  - "ai-protocol-mock/RELEASE_NOTES_v0.1.8.md"
  - "ai-lib-plans/active/projects/ai-protocol/tasks/PT-008-mock-release-and-runtime-smoke-validation.yaml"

testing:
  unit_tests: []
  integration_tests:
    - "ai-lib-rust: cargo test --test client_mock test_chat_completion_with_mock -- --ignored --nocapture"
    - "ai-lib-python: pytest tests/integration/test_mock_chat_e2e.py::test_chat_simple_via_mock"
    - "ai-lib-ts: npm run test -- tests/integration.test.ts -t 'should send chat request and get response'"
  compliance_tests: []

completion_notes: |
  Completed with release + verification report.

  Release:
  - ai-protocol-mock v0.1.8 committed and pushed to main.
  - GitHub release published: https://github.com/hiddenpath/ai-protocol-mock/releases/tag/v0.1.8

  Runtime smoke validation results:
  - ai-lib-rust:
    - Ran targeted mock integration test.
    - Result: failed with protocol validation error:
      "2.0 is not one of [1.1, 1.5]".
    - Interpretation: current rust runtime fixture/validator path does not yet accept
      protocol version 2.0 in this test flow.
  - ai-lib-python:
    - Initial run skipped due mock availability check using httpx with proxy env.
    - Forced run failed with environment/runtime dependency issue:
      "Using http2=True, but the 'h2' package is not installed."
    - Interpretation: test environment not ready for this e2e path (proxy + http2 deps).
  - ai-lib-ts:
    - Test suite failed during module load before test execution:
      "ReferenceError: FilterRule is not defined".
    - Interpretation: pre-existing runtime issue unrelated to mock release change.

  Conclusion:
  - Mock release is complete.
  - Cross-runtime smoke execution attempted and results captured.
  - Follow-up runtime-specific fixes should be tracked separately to clear the above blockers.

tech_considerations:
  architecture: |
    Verify downstream runtime behavior without coupling runtime release cadence.

  performance: |
    Smoke-only verification to keep iteration fast.

  security: |
    No new security surface expected; release only includes prior sync/tooling fixes.

  compatibility: |
    Validate backward compatibility via cross-runtime smoke runs.

constitution_rules:
  - "ARCH-001: Protocol-driven design"
  - "ARCH-003: Cross-runtime consistency"
  - "TEST-001: Compliance tests"
  - "DOC-001: Documentation language requirements"
---
