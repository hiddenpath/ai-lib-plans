---
id: "PT-066"
title: "ai-lib-go generative LLM capability adaptation"
status: "in_progress"
priority: "high"
assignee: "@ai"
project: "ai-protocol"
milestone: "v1.0.x"
labels:
  - "go"
  - "generative"
  - "runtime"
  - "adaptation"
created: "2026-03-30"
updated: "2026-04-21"
reviewed_codebases:
  - repo: "ailib-official/ai-lib-go"
    ref: "f4072c8"
    local_path_note: "d:/rustapp/ai-lib-go (representative clone)"
executor_name: "cursor-agent"
executor_terminal: "cursor-session"

description: |
  Adapt ai-lib-go to pass gen-001~gen-007 compliance fixtures.
  Go may lag by one sprint per PT-058 catch-up allowance.

  ## Gap Analysis (from audit)

  1. **reasoning_tokens in Usage** (gen-002): Usage struct only has 3 fields.
     → Add ReasoningTokens, CacheReadTokens, CacheCreationTokens fields.
       Parse from nested completion_tokens_details / Anthropic variants.

  2. **Streaming thinking/reasoning** (gen-006): sse.go only handles
     delta.content and delta.tool_calls; no thinking path.
     → Add reasoning_content / thinking block extraction for OpenAI and
       Anthropic SSE streams.

  3. **Structured output / response_format** (gen-003): ChatOptions has
     ResponseFormat map but Chat() does not include it in request JSON.
     → Wire ResponseFormat into Chat and ChatStream JSON payloads.

  4. **Tool call streaming** (gen-004): sseStream drops ToolCall field.
     → Surface ToolCall on StreamingEvent; implement partial accumulation.

  5. **feature_flags** (gen-001): Parsed into map[string]bool but not consumed.
     → Wire feature_flags into capability validation.

  6. **Compliance runner** (gen-001~007): No 08-generative-capabilities suite.
     → Add compliance/generative_test.go consuming shared YAML fixtures.

  ## Already working

  - Error classification context_length (gen-005): Handled (verify E1005).
  - MCP bridge (gen-007): Present and tested.

  ## Catch-up allowance

  Per PT-058, Go may lag one sprint on reasoning_tokens extraction
  and thinking stream support. If not at parity when PT-066 closes,
  document remaining items with explicit ETA.

execution_protocol: |
  MANDATORY: Test-first for every adaptation item.
  1. Write test covering expected behavior (gen-00x fixture assertion)
  2. Run test → must FAIL (proves test captures the gap)
  3. Implement minimal code to pass
  4. Run test → must PASS
  5. Run full go test ./... → no regression

acceptance_criteria:
  - "Usage struct includes ReasoningTokens"
  - "SSE decoder extracts thinking/reasoning blocks"
  - "Chat/ChatStream include ResponseFormat in request"
  - "StreamingEvent surfaces ToolCall with accumulation"
  - "compliance/generative_test.go passes gen-001 through gen-007"
  - "If partial: remaining items documented with ETA"
  - "Each adaptation item has evidence of test-fail-then-pass cycle"

dependencies:
  - "PT-057"
  - "PT-058"
related_issues: []
references:
  - "d:/rustapp/ai-lib-go/internal/stream/sse.go"
  - "d:/rustapp/ai-lib-go/pkg/ailib/types.go"
  - "d:/rustapp/ai-lib-go/pkg/ailib/client.go"
  - "d:/ai-protocol/tests/compliance/cases/08-generative-capabilities/"
estimated_hours: 16
actual_hours: null
blocks: []
blocked_by:
  - "PT-057"

files_changed: []

testing:
  unit_tests:
    - "ai-lib-go: gofmt -l . (no output); go vet ./...; go test ./... with COMPLIANCE_DIR pointing at ai-protocol/tests/compliance"
  compliance_tests:
    - "Existing compliance packages under tests/compliance (generative YAML runner still to add per acceptance list)"

completion_notes: |
  **Done on branch `pt-063-go-hardening` (2026-03-31, pushed):**
  Module path `github.com/ailib-official/ai-lib-go`; README quick start + license badge; CI workflow
  (gofmt, go vet, go test + COMPLIANCE_DIR); `buildChatPayload` shared by Chat and ChatStream (tools, tool_choice,
  response_format parity); `internal/protocol` JSON detection; non-stream `EnrichNonstreamChatResponse` /
  `response_paths` parity for reasoning text fallbacks (`pkg/ailib/chat_enrich.go`); streaming SSE decodes content +
  tool deltas (`internal/stream/sse.go`).

  **Correction vs earlier notes:** `pkg/ailib/types.go` defines `Usage` with **prompt/completion/total tokens only**
  (`json.Unmarshal` in `applyUsage` drops any extra provider fields). Extended reasoning/cache token **counts** are
  **not** modeled on `Usage` yet — do **not** treat “Usage extended for reasoning” as done until struct + JSON tags
  match PT-058 / gen-002.

  **Remaining:** OpenAI stream path does not surface `delta.reasoning_content` in `extractEventOpenAI`; Anthropic stream
  lacks a distinct “thinking” lane beyond text/tool fragments; accumulate partial tool args at the stream API layer;
  wire `feature_flags` from manifest into capability checks; add `tests/compliance/generative_test.go` (or equivalent)
  driving `08-generative-capabilities` YAML — align with PT-058 before closing.

  **Truth-source note (2026-04-21):** `status` remains `in_progress` until the remaining bullets are closed and
  verified. `active/ai-lib-status-report-2026-04-17.md` claiming full completion was **overstated** relative to
  this file. Active development targets **`ailib-official/ai-lib-go`** (remove any hiddenpath-only PR bookmarks).

  **Code review (2026-04-21) — ai-lib-go @ f4072c8 (GOV-002: smallest meaningful units):**
  - **Verified present:** `ChatOptions.ResponseFormat`; shared payload builder for Chat/ChatStream; SSE emits
    `ToolCallDelta` when OpenAI `delta.tool_calls` or Anthropic `input_json_delta`; `compliance_test.go` + other
    packages under `tests/compliance/`; manifest includes `feature_flags` in `internal/protocol/manifest.go` structs.
  - **Verified missing / incomplete vs acceptance_criteria:** `Usage` struct still three integer fields — no
    `ReasoningTokens` / cache splits; streaming decoder ignores OpenAI `reasoning_content`. No `generative_test.go`
    file found beside the shared compliance runner; no evidence in-tree of a dedicated 08-generative sweep.
  - **Status:** **`in_progress`** — matches corrected completion_notes above.

constitution_rules:
  - "ARCH-003: Cross-runtime consistency"
  - "GO-005: Manifest schema compatibility"
  - "GO-006: Transport stream contract"
  - "TEST-001: Compliance tests"
---
