---
id: "ZS-ML-003"
title: "Config: logical model id + fallbacks (Phase 2) — examples and regression tests"
status: "completed"
priority: "medium"
executor_name: "cursor-agent"
executor_terminal: "cursor-shell"
assignee: "@tbd"
project: "zerospider"
milestone: "ai-lib-migration"
labels:
  - "zerospider"
  - "config"
  - "ai-lib"
created: "2026-04-22"
updated: "2026-04-27"
description: |
  迁移计划 PR3/Phase2：用户配置以**逻辑 model id** 为主，并与现有
  `reliability.fallback_providers` / `model_fallbacks` 的关系写清楚。

  交付物：
  - `docs/ai-lib-migration.md` 或 `docs/` 下片段：**示例 TOML**（`default_provider` 为
    `openai/gpt-4o-mini` 等）+ `reliability` 与 protocol id 的对应关系。
  - 至少一个 **serde/配置层回归测试**：`ReliabilityConfig` 或主 `Config` 的 JSON/TOML
    样例在含 `provider/model` 字符串时可反序列化（不强制改 schema）。

acceptance_criteria:
  - "文档中有可复制粘贴的完整最小示例（含环境变量名引用）"
  - "新测试在 CI 中执行并通过"
  - "PR 已合并，YAML 中 pr 字段已回填"

pr:
  repo: "ailib-official/zerospider"
  branch_pattern: "feat/zs-ml-003-config-model-examples"
  merge_target: "main"
  branch: "feat/zs-ml-003-006-migration-wizard"
  compare_url: "https://github.com/ailib-official/zerospider/compare/main...feat/zs-ml-003-006-migration-wizard"
  new_pr_url: "https://github.com/ailib-official/zerospider/pull/new/feat/zs-ml-003-006-migration-wizard"
  url: "https://github.com/ailib-official/zerospider/pull/14"
  merge_commit: "aa3214a"

testing:
  unit_tests:
    - "config_toml_accepts_protocol_logical_ids_in_provider_and_reliability (src/config/schema.rs)"
  integration_tests: []
  evidence_on_complete:
    - "https://github.com/ailib-official/zerospider/pull/14 merge commit aa3214a (logical commit b35dc8b)"

related_plan_section: "PR3 — Phase 2"
blocked_by: []
blocks: []

completion_notes: |
  Delivered in combined PR #14 (merge commit aa3214a; logical commit b35dc8b).
  - docs/ai-lib-migration.md Phase2 minimal TOML for provider/model + [reliability].
  - src/config/schema.rs: config_toml_accepts_protocol_logical_ids_in_provider_and_reliability.
