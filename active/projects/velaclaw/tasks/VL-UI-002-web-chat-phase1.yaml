---
id: "VL-UI-002"
title: "VelaClaw Web Chat UI — Phase 1a: Local Control API (REST + WebSocket)"
status: "completed"
priority: "medium"
executor_name: "Spider"
executor_terminal: "opencode"
assignee: "@tbd"
project: "velaclaw"
milestone: "web-chat-ui"
labels:
  - "velaclaw"
  - "ui"
  - "websocket"
  - "gateway"
  - "local-control-plane"
created: "2026-06-10"
updated: "2026-06-17"
description: |
  Phase 1a of VL-UI-001 (Path 2).  Add Local Control Plane endpoints to the
  existing VelaClaw gateway: non-streaming chat REST, WebSocket streaming,
  provider metadata, and bearer auth aligned with PairingGuard.

  Chat execution MUST invoke the same agent strategy path as `velaclaw agent`
  (Agent::turn / tool loop) — NOT a parallel Provider call in gateway handlers.
  This satisfies VL-ARCH-001 (BYOK in-process, no Prism Gateway HTTP for chat).

  Frontend SPA is VL-UI-005 (separate PR).  Multi-crate: new modules under
  src/gateway/ (e.g. chat_api.rs, ws.rs) — no new workspace crate in Phase 1.

acceptance_criteria:
  - "POST /api/chat returns full response via agent loop (content, usage, cost)"
  - "GET /ws upgrades to WebSocket; streams delta + done events per VELACLAW_PHASE_UI_PLAN contract"
  - "GET /api/providers returns protocol_registry models with availability (env/keyring); secrets never exposed"
  - "/api/chat and /ws require PairingGuard bearer when pairing is active (same as /webhook)"
  - "Default gateway bind remains loopback-friendly; public bind documented as hazardous"
  - "Unit or integration tests for chat handler (mock provider or fixture)"
  - "cargo test + clippy pass; PR merged to ailib-official/velaclaw main"

pr:
  repo: "ailib-official/velaclaw"
  branch_pattern: "feat/vl-ui-002-*"
  merge_target: "main"
  branch: "feat/vl-ui-002-local-control-api"
  url: "https://github.com/ailib-official/velaclaw/pull/55"
  compare_url: "https://github.com/ailib-official/velaclaw/compare/main...feat/vl-ui-002-local-control-api"
  merge_commit: "7cb2528"

testing:
  unit_tests:
    - "Gateway chat handler: request validation, auth rejection without bearer"
    - "WebSocket protocol: delta/done frame serialization"
  integration:
    - "Manual: POST /api/chat with bearer, BYOK provider configured"
    - "Manual: /ws streaming with deepseek or mock"
  evidence_on_complete:
    - "PR #55 merged into ailib-official/velaclaw main (squash commit 7cb2528)"
    - "CI: rust-protocol-required check SUCCESS"
    - "cargo fmt --check: PASS (zero output)"
    - "cargo clippy: PASS (commit da9d429 fixes unused imports)"
    - "cargo test: PASS (via CI)"
    - "GOV-001: no hiddenpath URLs in new files"
    - "No unsafe/unwrap/panic in new code"
    - "No secret logging in new code"
    - "VL-ARCH-001: Agent::from_config + turn (no Provider shortcut)"

blocked_by:
  - "VL-UI-001"
blocks:
  - "VL-UI-005"

completion_notes: |
  Squash-merged 2026-06-17. Review verdict: APPROVE with no critical issues.
  - All VL-UI-002 acceptance criteria satisfied.
  - Architecture: Agent::turn path (VL-ARCH-001 D6) — no Provider shortcut in gateway.
  - Observer hooks deferred to VL-EVO-001 (known gap, acceptable per Phase 1 scope).
  - apply_chat_overrides: bare model_id sets default_model but not default_provider; config fallback applies.
  - WS_CHUNK_SIZE=48 (character-level UX adequate).
  - PAT scope insufficient for gh pr review/comment; posted review report to /tmp/review_velaclaw.md (local).
---

# VelaClaw Web Chat UI — Phase 1a — VL-UI-002

## Scope

### Backend — New gateway modules

Prefer new files under `src/gateway/` rather than growing `mod.rs` monolith:

| Module | Responsibility |
|--------|----------------|
| `chat_api.rs` | POST `/api/chat`, request/response types |
| `chat_ws.rs` | GET `/ws` upgrade, session stream |
| `providers_api.rs` | GET `/api/providers` |

### Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/chat` | POST | Non-streaming. Body: `{ messages, model_id?, temperature?, max_tokens? }`. Uses **agent loop**. |
| `/ws` | GET | WebSocket streaming chat. See `VELACLAW_PHASE_UI_PLAN_2026-06.md` §4. |
| `/api/providers` | GET | Logical models from `protocol_registry` + `available` flag (PT-074 credential chain). |

**Not in this task**: GET `/chat` SPA (VL-UI-005), memory/config/cron APIs.

Existing routes (`/health`, `/dashboard`, `/pair`, `/webhook`, …) unchanged.

### Architecture rules (VL-ARCH-001)

1. **No duplicate execution** — handlers delegate to `Agent` / existing `run_tool_call_loop`, not raw `Provider::chat`.
2. **BYOK** — credentials via ai-lib PT-074; gateway never accepts API keys in JSON body.
3. **Observer hook** — wire `ObserverEvent` so UI can later show tool progress (emit `tool_start`/`tool_end` on WS when ready).
4. **EVO-001** — may land in parallel; when `ExecutionHandle` exists, chat handlers switch to it without breaking WS contract.

### Auth

- Reuse `PairingGuard::authorize` pattern from webhook handlers.
- Unauthenticated → 401 with hint to `POST /pair`.

### API Contract

**POST /api/chat**
```json
// Request
{"messages":[{"role":"user","content":"hello"}],"model_id":"deepseek/deepseek-v4-pro","temperature":0.7}
// Response
{"id":"chat_xxx","content":"...","usage":{"input_tokens":10,"output_tokens":5},"cost":0.000035}
```

**WebSocket** — see `VELACLAW_PHASE_UI_PLAN_2026-06.md` §4.

### Out of scope

- rust-embed / Svelte (VL-UI-005)
- Session persistence across reload (VL-UI-003)
- Tool approval UI (VL-UI-004; WS event stub ok)

## Suggested PR split

Single PR acceptable if reviewable; optional sub-commits: (1) types + auth, (2) REST, (3) WS.
