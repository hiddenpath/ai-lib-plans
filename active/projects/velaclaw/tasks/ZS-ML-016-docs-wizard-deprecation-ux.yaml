---
id: "ZS-ML-016"
title: "Docs + wizard/doctor UX: protocol-only path, deprecation messaging, smoke checklist"
status: "completed"
priority: "medium"
executor_name: "composer-agent"
executor_terminal: "cursor-agent-p7-zsml"
assignee: "@ai"
project: "zerospider"
milestone: "ai-lib-migration-phase7"
labels:
  - "zerospider"
  - "docs"
  - "ux"
created: "2026-05-08"
updated: "2026-05-10"
description: |
  Align README, migration guides, and runtime errors so users are always steered to
  `provider/model` + `AI_PROTOCOL_DIR`. Update doctor / CLI paths when legacy keys
  are detected post-ZS-ML-015.

  Refresh docs contract tests if present.

acceptance_criteria:
  - "README quickstart uses protocol path only"
  - "docs/migration-legacy-to-protocol.md states removal version and last legacy-capable version if applicable"
  - "PR template manual checklist for fresh-clone protocol smoke (or scripted)"
  - "docs_contract tests green if repo has them"

pr:
  repo: "ailib-official/zerospider"
  merge_target: "main"
  branch_pattern: "docs/zs-ml-016-protocol-only-ux"
  url: "https://github.com/ailib-official/zerospider/pull/33"
  merge_commit: "21831717d39b22f924e792dea18c72569590a2c0"

blocked_by:
  - "ZS-ML-015"

completion_notes: | 
  PR #33 opened after ZS-ML-015 merge/backfill. It aligns runtime fallbacks, onboarding, Docker/bootstrap examples, README/docs references, and the PR template around protocol-only `provider/model` ids backed by `AI_PROTOCOL_DIR`. 
  
  Review round 1 (2026-05-10): Two ⏳ items raised — (I1) hardcoded `"openai/gpt-5.2"` in 7+ locations, (I2) Tier 2 gateway placeholder IDs with no manifests. Developer fixed both: I1 → new `src/config/defaults.rs` with `pub const DEFAULT_PROTOCOL_MODEL_ID`, all Rust files now use the constant; I2 → Tier 2 trimmed to Bedrock only + `print_gateway_tier_manifest_hints()` explaining manifest requirement. All issues resolved, CI green, squash-merged 2026-05-10.

  Merge commit: squash-merged via `gh pr merge --squash` on 2026-05-10T16:03:35Z.

testing:
  evidence_pr_33_local_2026_05_09: |
    PASS: `cargo fmt --all -- --check`
    PASS: `cargo check -p zerospider --features ai-protocol --locked`
    PASS: `cargo test -p zerospider --features ai-protocol --locked --lib`
    PASS: `cargo test -p zerospider --features ai-protocol --locked --test docs_contract_contributing`
    PASS: `cargo clippy -p zerospider --all-targets --features ai-protocol --locked -- -D warnings`
    PASS: `git diff --check`
    PASS: `rg "--provider openrouter|default_provider = \"openrouter\"|PROVIDER=openrouter|custom:https://your|anthropic-custom:https://your|28 built-in"` returned no matches.
  evidence_pr_33_ci_2026_05_10: |
    PASS: rust-protocol-required (GitHub Actions run 25626310037)
    PASS: All ⏳ review items resolved (I1: constant centralization, I2: gateway manifest hints)

related_plan: "active/projects/zerospider/ZEROSPIDER_PHASE7_LEGACY_ELIMINATION_PLAN_2026-05-08.md#zs-ml-016--文档向导与用户体验"
