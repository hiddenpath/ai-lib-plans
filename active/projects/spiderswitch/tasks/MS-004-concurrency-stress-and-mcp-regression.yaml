---
id: "MS-004"
title: "后发布加固：并发切换压力测试与MCP回归测试"
status: "completed"
priority: "high"
assignee: "@hiddenpath"
project: "spiderswitch"
milestone: "v0.2.1"
labels:
  - "test"
  - "stability"
  - "mcp"
created: "2026-03-02"
updated: "2026-03-02"

description: |
  在 v0.2.0 发布后，补齐稳定性验证深度：
  1) 增加并发切换压力测试，验证 runtime 切换互斥与状态一致性
  2) 增加 MCP 路径回归测试，覆盖关键错误路径与状态返回契约
  3) 形成可重复执行的回归基线，支持后续版本持续验证

acceptance_criteria:
  - "新增并发切换测试，能稳定复现并验证互斥语义"
  - "新增 MCP 回归测试，覆盖未知工具/状态返回关键路径"
  - "测试套件通过（ruff + mypy + pytest）"
  - "测试设计和结果回写到 standup 与任务卡"

dependencies:
  - "MS-003"
related_issues: []
references: []
estimated_hours: 4
actual_hours: 1
blocks: []
blocked_by: []

files_changed:
  - "tests/test_runtime_concurrency.py"
  - "tests/test_server_regression.py"

testing:
  unit_tests:
    - "python -m pytest -q -> 21 passed"
  integration_tests:
    - "python -m ruff check src tests -> All checks passed"
    - "python -m mypy src -> Success: no issues found in 13 source files"
  compliance_tests: []

completion_notes: |
  已完成首批后发布测试加固：
  1) 新增并发切换测试，验证 switch lock 串行语义（max_active == 1）
  2) 新增 MCP 回归测试，覆盖参数脱敏、list 参数类型收敛、status 协调字段、switch 缺参错误结构
  3) 测试基线已提升为 21 tests，全量通过

tech_considerations:
  architecture: |
    测试仅验证行为契约，不改变核心架构边界。
  performance: |
    压测规模控制在本地可重复执行范围，避免CI抖动。
  security: |
    回归测试覆盖敏感参数脱敏路径，避免日志泄露回归。
  compatibility: |
    测试以外部行为为准，避免绑定内部实现细节。

constitution_rules:
  - "ARCH-001: Protocol-driven design"
  - "ARCH-003: Cross-runtime consistency"
  - "DOC-001: Documentation language requirements"
  - "TEST-001: Compliance tests"
---
