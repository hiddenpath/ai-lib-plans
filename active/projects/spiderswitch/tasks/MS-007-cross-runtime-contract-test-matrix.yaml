---
id: "MS-007"
title: "跨运行时契约测试矩阵：能力分发与路由语义一致性"
status: "completed"
priority: "high"
assignee: "@hiddenpath"
project: "spiderswitch"
milestone: "v0.4.x"
labels:
  - "test"
  - "runtime-routing"
  - "contract"
created: "2026-03-06"
updated: "2026-03-07"

description: |
  建立跨运行时契约测试矩阵，验证 Python/Rust/TS（并为 Go/WASM 预留）
  在同输入条件下的路由语义一致性，重点覆盖能力分发、状态返回、错误归类、
  回退语义和可观测字段的一致行为。

acceptance_criteria:
  - "定义核心测试矩阵：model capability × runtime capability × routing mode"
  - "覆盖 switch/list/status/reset 关键契约字段一致性"
  - "对 error type/code/details 建立跨运行时对齐断言"
  - "产出未来 Go/WASM 适配所需的最小契约校验清单"
  - "将测试结果回写到 standup 与任务卡"

dependencies:
  - "MS-006"
related_issues: []
references:
  - "reviews/audits/SPIDERSWITCH_RUNTIME_ROUTING_REPORT_CN.md"
  - "MEMORY.md"
estimated_hours: 12
actual_hours: 3
blocks: []
blocked_by: []

files_changed:
  - "spiderswitch/tests/test_runtime_registry.py"
  - "spiderswitch/tests/test_server_regression.py"
  - "spiderswitch/tests/test_state.py"
  - "spiderswitch/tests/test_tools.py"
  - "spiderswitch/tests/test_runtime.py"
  - "active/projects/spiderswitch/RUNTIME_ROUTING_EXECUTION_WAVE1_2026-03-07.md"

testing:
  unit_tests: []
  integration_tests: []
  compliance_tests: []

completion_notes: |
  Completed on 2026-03-07.
  - Established contract matrix baseline for runtime selection semantics:
    request runtime_id > state runtime_id > default runtime.
  - Added assertions for switch/list/status/reset runtime-aware fields and scoped reset behavior.
  - Added runtime schema assertions for reserved runtimes (Go/WASM ready).
  - Validation evidence: `python -m pytest` passed in spiderswitch (32 passed).

tech_considerations:
  architecture: |
    契约测试是跨语言演进护栏，优先保证语义一致而非内部实现一致。
  performance: |
    契约用例应分层：快速回归集 + 完整矩阵集，避免 CI 过慢。
  security: |
    测试样例不得包含真实密钥与敏感信息。
  compatibility: |
    兼容现有 Python 输出结构，同时为新 runtime 字段做可选断言。

constitution_rules:
  - "ARCH-003: Cross-runtime consistency"
  - "DOC-001: Documentation language requirements"
  - "TEST-001: Compliance tests"
---
